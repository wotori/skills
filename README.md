# skills

Local Codex skills source folder.

## Install

```sh
make install
```

This copies every child folder that contains `SKILL.md` into `~/.codex/skills` and makes a best-effort copy into other local agent skill folders when present.
